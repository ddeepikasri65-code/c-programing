
#include <stdio.h>
#include <math.h>
int main() {
      float area,length;
      printf("Enter the area:\n");
      scanf("%d",&area);
      length = sqrt(area);
      printf("The lenght is %.2f",length);
    return 0;
}